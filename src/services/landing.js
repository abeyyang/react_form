import React from 'react';
import apiConfig from '../../config/apiConfig';
import _ from "lodash";

const api = {
    testLanding(){
        let result = {
            testResult: "good"
        };
        return result;
    }
}