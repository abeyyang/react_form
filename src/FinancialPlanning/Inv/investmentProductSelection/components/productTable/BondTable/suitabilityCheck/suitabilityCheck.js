import React, { Component } from 'react';
import styles from './style.scss';

const suitabilityCheckBond = (props)=>{
    const { data,row,column} = props;
    return (
        <div>
            <span></span>
        </div>
    );
};



export default suitabilityCheckBond;
